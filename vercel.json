{
  "installCommand": "cd backend && npm install && cd ../frontend && npm install",
  "buildCommand": "cd backend && npx tsc && cd ../frontend && npm run build && mkdir -p ../backend/frontend-dist && cp -r dist/* ../backend/frontend-dist/"
}
