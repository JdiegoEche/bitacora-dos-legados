{
  "installCommand": "npm install && cd ../frontend && npm install",
  "buildCommand": "npx tsc && cd ../frontend && npm run build && mkdir -p ../backend/frontend-dist && cp -r dist/* ../backend/frontend-dist/"
}
