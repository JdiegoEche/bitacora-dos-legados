{
  "installCommand": "npm install && cd ../frontend && npm install",
  "buildCommand": "npx tsc && cd ../frontend && npm run build && mkdir -p frontend-dist && cp -r ../frontend/dist/* frontend-dist/"
}
