{
  "name": "bitacora-dos-legados",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "start": "NODE_ENV=production node backend/dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
